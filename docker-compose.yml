services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - APP_ENV=development
      - SECRET_KEY=change-me
      - DATABASE_URL=sqlite:///k2_orders.db
    volumes:
      - .:/app
